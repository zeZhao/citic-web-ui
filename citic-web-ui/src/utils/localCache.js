/**
 * 本地缓存方法，请在注释中注明KEY值的意义
 */
let LocalCache =  new Object();
/**
 * key值列表及说明
 * citic-token:citic-token;
 */
// //设置本地持久缓存
// LocalCache.getLastingIteam = (key)=>{
//     return sessionStorage.getItem(key)
// }
// //获得本地持久缓存
// LocalCache.setLastingIteam = (key,val)=>{
//     sessionStorage.setItem(key,val);
// }
// //移除本地持久缓存
// LocalCache.removeLastingItem = (key)=>{
//     sessionStorage.removeItem(key)
// }

// //设置本地临时缓存
// LocalCache.getSessionIteam = (key)=>{
//     return sessionStorage.getItem(key)
// }
// //获得本地临时缓存
// LocalCache.setSessionIteam = (key,val)=>{
//     sessionStorage.setItem(key,val);
// }
// //移除本地临时缓存
// LocalCache.removeSessionItem = (key)=>{
//     sessionStorage.removeItem(key)
// }
export default LocalCache



